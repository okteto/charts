name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Helm 3
      uses: WyriHaximus/github-action-helm3@v1.0.0
      with:
        exec: ./generate.sh
