jenkins:
  master:
    resources:
      requests:
        cpu: "800m"
        memory: "256Mi"
      limits:
        cpu: "1000m"
        memory: "1024Mi"
    ingress:
      enabled: true
      annotations:
        dev.okteto.com/generate-host: "true"
